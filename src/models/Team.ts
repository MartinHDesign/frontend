export class Team {
    id: string;
    name: string;
    logo: string;

    constructor(id: string, name: string, logo: string){
        this.id = id;
        this.name = name;
        this.logo = logo;
    }
}